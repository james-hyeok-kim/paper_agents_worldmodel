# worldmodel-idea-generator Memory

아이디어 생성 이력. 각 항목은 `pending/<slug>.md` 또는 `active/<slug>.md`를 가리킨다.

## 인덱스

<!-- 새 아이디어 추가 시 아래에 한 줄 추가 -->
<!-- - [<Slug>](pending/<slug>.md) — <한 줄 요약> | status: pending | category: X | venue: Y -->

### 2026-06-11 KST 세션 (efficiency 5종)
- [predictive-horizon-controller](pending/predictive-horizon-controller.md) — RSSM 내부 prior-entropy/reward-var로 per-state adaptive horizon, rollout 1.6~2× | status: pending | category: A | venue: ICLR
- [latent-delta-rollout](pending/latent-delta-rollout.md) — h delta 작은 step에서 GRU update skip(deterministic만), forward 30~50%↓ | status: pending | category: B | venue: NeurIPS
- [branch-shared-imagination](pending/branch-shared-imagination.md) — 병렬 imagination/MPPI 분기를 KL로 merge, planning forward 2~3×↓ | status: pending | category: D | venue: ICLR/CoRL
- [dual-rate-world-model](pending/dual-rate-world-model.md) — slow/fast latent 분해, slow는 K step마다 갱신, dynamics 35~55%↓ | status: pending | category: E | venue: NeurIPS
- [static-dynamic-decoupled-wm](pending/static-dynamic-decoupled-wm.md) — static latent 에피소드당 1회 인코딩+broadcast, encode/decode 25~45%↓ | status: pending | category: G | venue: ICLR/CoRL

### 2026-06-11 KST 세션 2 (efficiency 5종, BL-06~09 실패 회피 + Amdahl filter)
- [selective-imagination-cheap-critic](pending/selective-imagination-cheap-critic.md) — latent-only value-variance critic이 batch축에서 imagine할 trajectory만 선택(시간축 truncation 아님→cliff-free), imagination 40~55%↓ | status: pending | category: C | venue: NeurIPS/ICLR
- [iris-token-budget-rollout](pending/iris-token-budget-rollout.md) — IRIS autoregressive rollout에서 input-side router가 token별 layer-depth 가변(early-exit/MoD, 결정이 forward 선행→entropy 함정 회피, BL-09 redirect), transformer FLOPs 45~60%↓ | status: pending | category: C | venue: NeurIPS/ICLR
- [residual-corrected-sparse-rssm](pending/residual-corrected-sparse-rssm.md) — carry-forward(BL-06) 대신 rank-r residual corrector가 Δh 능동 추정+re-anchor로 drift 차단, GRU 35~50%↓(prior MLP는 매 step 유지) | status: pending | category: B | venue: NeurIPS/ICLR
- [subtree-reuse-muzero](pending/subtree-reuse-muzero.md) — discrete-action MCTS(MuZero)에서 inter-step subtree carry-over + value-staleness 보정/stochastic outcome reconcile(BL-07 redirect: merge 아닌 step간 재활용; intra-search는 이미 캐시됨), dynamics forward 25~40%↓ | status: pending | category: D | venue: NeurIPS/AAAI
- [policy-gated-progressive-wm](pending/policy-gated-progressive-wm.md) — cheap/exp 2-tier WM을 policy-visitation/value-gap gating으로 conditional routing(BL-04 redirect: 단일 student 압축 아님), transition 40~55%↓ | status: pending | category: F | venue: NeurIPS/ICLR/CoRL

### 2026-06-11 KST 세션 3 (efficiency 5종, dual-rate 성공패턴 확장 + call-skip 원칙 + 가정 분산)
- [amortized-rollout-operator](pending/amortized-rollout-operator.md) — sequential H-step GRU call을 policy-흡수 amortized operator로 single parallel pass(closed-loop blocker 정면돌파, online distillation+trust-region), imagination 2.5~4× | status: pending | category: D | venue: NeurIPS/ICLR
- [spectral-multilevel-rssm](pending/spectral-multilevel-rssm.md) — 차원별 temporal autocorrelation→spectral clustering으로 L-band update-frequency 자동할당(dual-rate 2-tier→데이터기반 multi-band, prior+GRU 함께 band-skip), dynamics call 40~60%↓ | status: pending | category: E | venue: NeurIPS/ICLR
- [tokenizer-stride-iris](pending/tokenizer-stride-iris.md) — IRIS 진짜 bottleneck인 tokenizer(VQVAE 76%)를 frame-stride로 call skip, token-space rollout+token-space reward head+K-frame decode anchor(BL-13 redirect), imagination 1.8~2.5× | status: pending | category: G | venue: NeurIPS/ICLR
- [value-bootstrapped-depth-scheduler](pending/value-bootstrapped-depth-scheduler.md) — pre-rollout value-curvature로 per-state depth 결정+잘린 tail은 value bootstrap(truncation 아님→cliff-free, BL-08 redirect: per-step gate 아님/dense label), depth 35~50%↓ | status: pending | category: A | venue: NeurIPS/ICLR
- [action-quantized-planning-cache](pending/action-quantized-planning-cache.md) — TD-MPC2 MPPI에서 중복 (s,a) transition을 LSH 양자화로 batch-dedup memoize(merge 아님→발산 무관, BL-07 redirect)+elite full-forward 안전장치, planning 1.6~2.4× | status: pending | category: D | venue: NeurIPS/CoRL

### 2026-06-18 KST 세션 4 (pivot — transformer/diffusion/JEPA WM family 전환)
- [round4-pivot-saturation-map](pending/round4-pivot-saturation-map.md) — pivot 아이디어 5개 요청에 7개 probe 전부 포화 확인(clean survivor 0~1). cell별 차단논문 기록(DISK/2602.18639/C-JEPA/MaPER·UPER/EDELINE/Self-Forcing·Epona/Vid2World). hot-path 검증으로 STORM(transformer가 cheap, single fused token)·DINO-WM(rollout hot-path cite불가) 탈락. 메타교훈: pending-fresh≠literature-fresh, hot-path는 cite로만, monolithic U-Net spatial-selective는 unsound, collision-padding 금지 | status: saturation-report | category: META

### 2026-06-18 KST 세션 4b (DreamerV3 quality/sample-eff pivot — 방향 재확정 후)
- [learned-kl-reliability-lambda](pending/learned-kl-reliability-lambda.md) — DreamerV3 imagination은 prior-only(posterior-prior KL 부재, 코드확인)라, KL을 *학습된 predictor head*로 prior-only proxy화→λ-return의 step별 신뢰 weighting(truncation 아님→BL-08 cliff 회피, entropy aleatoric 함정도 회피). ensemble-free RSSM-내부 신호(dual-rate 성공패턴). forward 안 줄임→GPU/Amdahl 무관. Gate B=fixed-step return +8~15% 예상 | status: pending | category: quality/sample-eff | venue: NeurIPS/ICLR
  - 미생성(literature-blocked, collision 회피): 멀티샘플 critic target→Probabilistic Dreaming(2603.04715) 선점 / reward-relevant capacity→value-equivalence(VAML)+MuDreamer 선점 / prioritized replay→MaPER·UPER 선점 / JEPA planning-rep→2602.18639·C-JEPA 선점.
  - 메타교훈 확장: quality/sample-eff 축은 family-pivot보다 덜 포화됐으나 여전히 worked(4/5 cell 선점). imagination 신호 설계 전 prior-only 제약을 코드로 먼저 확인할 것.
  - **PoC 결과 (BL-14 등재)**: signal validity(critic_spread가 drift 예측)는 synth에서 PASS였으나 reweighting 메커니즘이 oracle 신호로도 state-agnostic baseline 무패 → 무이득. 실물 DreamerV3 H=15에선 imagination이 충분히 정확해 premise-collapse. 교훈: **"신호가 valid"≠"그 신호로 고치면 이득"** — feasibility는 oracle+trivial-baseline 통제로 별도 검증.

### 2026-06-19 KST 세션 5 (robustness/OOD/eval-diagnostic pivot)
- [decision-fidelity-atlas](pending/decision-fidelity-atlas.md) — eval-diagnostic. 표준 DreamerV3 imagination이 state-MSE가 아니라 *action-ordering(advantage 순서)*을 보존하는지 horizon×proprio/vision×in-dist/OOD 격자에서 진단(Kendall τ), DF를 dynamics기여(DF_dyn) vs reward-head기여(DF_rew)로 분해. NULL 사전등록(진단이 negative 날 수 있게), reliability-reweighting과 hard firewall(BL-14 재유입 금지). 서베이(2510.16732)가 imagination 진단 부재 공인 | status: pending | axis: eval-diagnostic | venue: NeurIPS D&B/ICLR
  - 미생성(documented): OOD(contextual-WM cell)→cRSSM/DALI/Many-Worlds 선점 / action-noise robustness→PR-MDP·NR-MDP+WM-specific 메커니즘 부재 / sensor-noise→Surprise Recognition(2512.01119) / RSSM multimodal-collapse 진단→문서화된 limitation+Probabilistic Dreaming 충돌 / calibration 진단→실패모드 문서화됨+atlas와 collapse.
  - 메타교훈: eval-diagnostic이 셋 중 가장 fresh(서베이 공인 gap). 진단 아이디어의 feasibility 게이트는 "valid measurement"가 아니라 **"non-obvious AND 결정을 바꿈"** + NULL 사전등록(진단이 실패 가능해야 테스트). 과거 실패(BL-14)를 깔끔히 설명하는 narrative는 confirmation bias — 증거 아닌 *열린 질문*으로 reframe.
  - **후속(team-lead, 2026-06-19)**: decision-fidelity-atlas는 INCREMENTAL(margin 얇음)로 drop. 핵심 개념을 value-equivalence/Model-Advantage(2106.14080)가 선점. eval-diagnostic도 결국 saturated — atlas로 입증.

### 2026-06-19 KST 세션 6 (TD-MPC2/Puppeteer — 새 base 2종, planning hot-path)
- [elite-staged-value-planning](pending/elite-staged-value-planning.md) — TD-MPC2 efficiency(anchor). **Amdahl 코드검증**(6라운드 최강 precondition): `_plan`이 매 step iterations=6×_estimate_value, horizon=3에서 terminal Q-ensemble term ≈ per-sample 50%. cheap-rank(1~2 head/distilled)로 512 순위→full 5-head는 elite 64만(chicken-and-egg 수정: elite 찾으려면 512 value 필요). BL-07 merge 아님(발산 무관), REDQ/DC-MPC critic-subsample은 learning-bias용이라 차별. wall-clock gate(vectorization). | status: pending | base: TD-MPC2 | axis: efficiency | venue: CoRL/ICLR
- [adaptive-mppi-budget](pending/adaptive-mppi-budget.md) — TD-MPC2 efficiency(보완, orthogonal: dynamics term=나머지 50%). 수렴신호(elite-value var/action std/mean Δ, MPPI 내부서 이미 계산=관측가능→BL-08 함정 회피)로 iteration/sample per-state 조기종료(call-skip, dual-rate 패턴). 전제 "후반 iteration 잉여"는 EMPIRICAL-ONLY(die-point #1). BL-05 static sweep과 차별(per-state learned stop). | status: pending | base: TD-MPC2 | axis: efficiency | venue: CoRL/ICLR
  - 미생성(documented): MPPI trajectory reuse/merge→repo에 warm-start+policy-prior 이미 존재+BL-07 발산 / Puppeteer high-level 저빈도 replan→options/command-persistence heavily worked, interface 메커니즘 부재 / Q-disagreement quality→ensemble-pessimism·UPER 선점 / decoder-free OOD→WM-specific 메커니즘 부재.
  - 메타교훈: repo 자체를 pre-emption으로 먼저 확인(warm-start/policy-prior가 reuse-idea 선점). 두 idea가 *같은 cost equation의 다른 절반*을 쳐서 orthogonal(1/2-collision 회피).
  - **[정정 2026-06-19 R7] 이 entry의 원래 "fresh base = throughput 레버(R5 1→R6 2)" 주장은 FALSIFIED.** R6 두 idea 모두 死(elite-staged=BL-15 wall-clock FAIL, adaptive-budget=parked). generation은 1→2로 올랐으나 **survival은 0 유지**. + "Amdahl 코드검증하면 planning-efficiency 살아남" 도 틀림 — Amdahl(FLOPs)은 맞았으나 vmap vectorization 때문에 wall-clock 무이득(BL-15). 교훈 정정: **FLOPs Amdahl ≠ wall-clock 이득**, base 구조(vmap/sequential)를 생성단계서 확인. bottleneck은 generation 아니라 **survival**(wall-clock + literature gate). [[round7-nonplanning-saturation-map]]

### 2026-06-19 KST 세션 7 (TD-MPC2 non-planning) — 0 clean (전부 선점)
- [round7-nonplanning-saturation-map](pending/round7-nonplanning-saturation-map.md) — TD-MPC2 비-planning 축 0 clean. cell별 named-paper 차단: robustness→RePo/TIA/Denoised-MDP(+distractor harness 부재, 이중차단) / consistency-representation→iQRL(2406.02696)+SPR/Tang(TD-MPC=self-predictive member)+SimNorm=Simplicial Embeddings / Q-ensemble value→REDQ·UPER·EDAC·DEA·TD-M(PC)² (fifth mechanism 부재) / pi-planning gap→TD-M(PC)² / multitask→One Model for All Tasks+gradient-surgery field. **Cross-round 합성: R4~R7 net survivable=0, bottleneck은 survival(generation 아님), "fresh base 추가"는 테스트되어 불충분(R6).** team-lead 결정지점 3개(efficiency-framing 중단/dual-rate 집중/새 problem-setting) | status: saturation-report
  - 메타교훈: 0 clean은 *specific named-paper block*일 때 valid 판정(reskin보다 나음, 단 "0이 미덕"이 아니라 "block이 specific해서 0"). efficiency idea는 wall-clock(GPU+CPU)을 생성단계서 가늠(BL-15). 7라운드 "local-fresh≠global-fresh" 반복 — cell의 named paper를 생성 전 찾아라.

### 2026-06-19 KST 세션 8 (least-worked corner, 사용자 "계속 생성") — 0 clean + 1 pointer
- [round8-openspace-pointer](pending/round8-openspace-pointer.md) — 0 clean mechanism-idea. **구조적 reframe(headline): freshness-check는 novelty를 *반증*은 해도 *입증*은 못 함** → genuinely-new problem-setting(option3)은 도구상 certify 불가. 모든 후보가 [clean check⟹mapped⟹死] 또는 [genuinely-new⟹literature 없음⟹de-risk 불가]로 갈리는 구조적 bind. valid open-space 판별은 literature-clean이 아니라 **3-part test(harness 존재/face-value 가치/non-mechanism)**. 기각: WM-of-frozen-subpolicy(diagnostic+harness 미검증+OHIO), Puppeteer transfer(자체 ICLR25 contribution+THICK/OHIO/MetaWorld). **THE ONE BET**: frozen low-level 하 high-level task acquisition sample-eff frontier(Puppeteer TransferWrapper harness 존재, 코드검증) — 단 diagnostic-shaped, de-risk 불가, validator의 non-obvious gate에서 산다/죽는다. | status: open-space-pointer
  - 메타교훈: **exotic framing을 발명해야 whitespace가 보이면 그 whitespace는 대개 측정불가/무가치라 비어있는 것(rationalization 신호)**. 8라운드 누적 survival=0 — 진짜 미테스트 lever는 human이 정의하는 새 problem-setting뿐(generator로 더 못 뽑음). EV: dual-rate 1편 > 죽을 새 idea.

### 2026-06-20 KST 세션 9 (cross-paradigm lever) — 0 clean (lever 닫힘)
- [round9-crossparadigm-confound](pending/round9-crossparadigm-confound.md) — team-lead가 3 base 대비(imagination/planning/hierarchical)를 새 lever로 제시. lead 후보(cross-paradigm per-state competence)를 4-bar 자가판정 → **bar 4(distinguishable)에서 死: Dreamer vs TD-MPC2 차이를 paradigm에 귀속 불가(두 codebase 교란)**. **R9 finding: 이 confound가 lever 전체를 닫음** — comparison⟹confound / combine⟹mechanism-mapped / Puppeteer-vs-flat는 Puppeteer ICLR25 선점. R8 bind의 cross-paradigm 인스턴스. | status: closed-lever-finding
  - 메타교훈: **cross-paradigm comparison은 attribution confound(다른 codebase)로 구조적으로 막힌다** — paradigm 결론엔 통제된 변형(같은 codebase, 한 축만) 필요, 독립구현 3종으로 불가. diagnostic 후보는 bar 4(distinguishable/귀속)가 가장 자주 죽이는 bar. **9라운드 누적: lever 3개(새 axis·새 base·cross-paradigm) 전부 테스트되어 닫힘.** forward: R8 Puppeteer bet을 validator 4-bar로 보내는 게 R10 생성보다 우선. EV: dual-rate > 새 idea.

### 2026-06-20 KST 세션 10 (첫 human-defined lever: DreamerV4+Puppeteer inference efficiency) — 0 clean (두 arm 닫힘)
- [round10-human-lever-efficiency](pending/round10-human-lever-efficiency.md) — **Arm A Puppeteer 2-level planning eff → event-triggered/self-triggered MPC(제어이론)로 선점.** 코드확인: Puppeteer는 진짜 2 MPPI/step(low가 별도 full TD-MPC2, transfer.py:86). high-level adaptive skip은 wall-clock 절감 실재지만 **novelty 차단**(R6에서 막힌 건 Amdahl 아닌 novelty). learned-trigger 변형조차 Ren2026/2208.10302 선점. **Arm B DreamerV4 → 저자 자기선점(shortcut forcing 헤드라인)+코드부재→wall-clock gate 불가(BL-15).** | status: closed-both-arms
  - 메타교훈: **efficiency 후보는 인접 *제어이론* 키워드(event-triggered/self-triggered/multi-rate MPC)로 freshness check**(crowded-field가 control). code finding이 Amdahl 확립해도 novelty 차단 안 풀림. 코드-부재 base(DreamerV4)는 wall-clock gate 불가→efficiency idea validator 통과 구조적 불가. **R10 핵심: human lever 형식은 맞으나 *efficiency* framing이 graveyard — dual-rate(survivor)는 efficiency 아닌 quality로 이김.** efficiency 死/quality 生 패턴 확정. arm을 몰래 quality로 안 바꿈(human이 efficiency 명시)→재조준 표면화. lever 4개째 테스트됨; 남은 건 human-defined + quality/sample-eff/capability framing(= R8 acquisition-curve bet 공간, validator 미통과).

### 2026-06-20 KST 세션 11 (DreamerV3+Puppeteer inference efficiency, 둘 다 runnable) — 0 by construction
- [round11-dreamerv3-puppeteer-efficiency](pending/round11-dreamerv3-puppeteer-efficiency.md) — 사용자가 DreamerV4→DreamerV3 교체(runnable). 결과 0, search 불필요. **핵심 kill: DreamerV3 arm은 saturation이 아니라 construction으로 0** — 코드확인(`dreamer.py:94 _policy`) inference=encoder+RSSM+actor 1 forward, **decoder/planning 없음**→이미 쌈. 비용은 전부 training(=BL-06~13+reconstruction-free R7). DreamerV3는 inference-eff 대상이 구조적으로 아님. 4-wall: DreamerV3 near-empty / Puppeteer event-triggered-MPC(R10) / 통합 substrate 없음 / microbench 미도달. drift("decoder training cost") 차단=off-lever+reconstruction-free 선점. | status: closed-by-construction
  - 메타교훈: **"inference efficiency" 요구 시 base의 inference 경로를 코드로 먼저 보라** — phase별 비용이 base마다 다름(DreamerV3=training-heavy/inference-cheap, TD-MPC2/Puppeteer=inference-heavy planning). **construction("hot-path가 애초에 없음")이 saturation보다 깨끗한 0.** **R11 binding constraint = validator wedge: R9·R10·R11 모두 "R8 bet→validator" 권고했으나 미실행(validator wedge). 이제 진짜 막힌 건 라운드 0이 아니라 유일 testable asset이 gate에 못 감.** → escalation: validator unblock이 11번째 생성보다 우선. lever 5개째 닫힘.

### 2026-06-22 KST 세션 12 (efficiency 1개 요청, dual-rate parity 종결 직후) — 0 valid closure
- [round12-efficiency-closure](pending/round12-efficiency-closure.md) — **efficiency framing 소진 확정.** 4관문 판별테스트(G1 wall-clock hot-path / G2 BL-free / G3 named-paper-free / G4 GPU vmap·kernel 생존) 동시통과 후보 0. **G4가 결정 kill: GPU 실질 speedup=call-skip만 유효(BL-10/12/15)인데 call-skip 대표 dual-rate=2026-06-22 parity 종결(efficiency-quality sweet spot 구조적 부재).** 6후보 직접 재구성 전부 死(STORM=cheap fused token / decoder=recon-free선점 / dynamics병렬=AOP선점+unsound / RSSM-adaptive-horizon=landscape-gap이지만 BL-08/14 feasibility死 / MPPI조기종료=AOP1912.01188 / Q-ensemble=vmap kernel-bound). 5-lever(new-axis/new-base/cross-paradigm/human-eff-lever/runnable-eff) 전부 닫힘. **공급≠throughput 데이터증거: pending 24개+conditional-go verdict 18개 적체인데 survival=0 → validator wedge가 binding(R9·R10·R11·R12 4번째 escalation).** | status: valid-closure
  - 메타교훈: **literature-fresh ≠ feasibility-fresh** (landscape의 RSSM-adaptive-horizon gap이 BL-08/14로 死인 게 표본 — gap기록을 생성근거로 쓰기 전 feasibility 이력 교차확인). **efficiency는 메커니즘이 아니라 framing 단위로 죽음**(G4를 어떤 신규 메커니즘도 못 넘음). **binding=게이트 throughput(validator wedge)이지 생성기 공급 아님** — 12번째 생성은 같은 wedge에 막힘. forward: (1)validator wedge 해소 최우선 (2)미테스트 lever=quality/sample-eff/capability의 human-defined problem-setting(R8 bet 공간, efficiency로 rebrand 금지). efficiency死/quality生 패턴 확정.

### 2026-06-22 KST 세션 13 (R13, non-efficiency b1/b2/b3 전부 열림, 사용자 "가장 강한 1개") — 1 생성 (efficiency 종결 후 첫 survivor 후보)
- [embedding-only-task-onboarding](pending/r13_embedding-only-task-onboarding.md) — TD-MPC2 frozen multitask checkpoint(mt30/mt80, 공개)에서 held-out task를 **96-dim task embedding만 학습(backbone frozen)**으로 onboard. claim-led: in-manifold held-out에서 embed-only≈full-FT(96≈backbone 붕괴) + dynamics-similarity 지표가 breakdown boundary 예측. **collision cleared**: TD-MPC2는 full-FT만+embed=init용(저자 "open problem" 명시)=CLEAR; CaDM/HiP-MDP/PEARL은 jointly-small/policy-cond=PARTIAL(firewall: frozen-large-OOD-boundary + dynamics-capacity). **bar4(distinguishable) PASS**=R9 cross-codebase confound 회피(같은 codebase, 한 축=embedding만 변경). 유일 risk=bar2 surprise(PoC로만, NULL 사전등록). | status: pending | direction: b1/b2 | base: TD-MPC2 | venue: NeurIPS/ICLR/CoRL
  - 메타교훈: **efficiency 종결(R12/12b) 후 첫 생성 survivor 후보** — non-efficiency lever(b1/b2)가 efficiency와 달리 GPU-wall 무관(forward 불변, training-time sample-eff). harness-existence가 b3 게이트(R8): 공개 mt checkpoint 확인=runnable today(80-task 재학습 불필요). 진짜 게이트는 literature 아니라 **shape**: "measure+characterize"=diagnostic(decision-fidelity/R8 死 shape)→claim-led(baseline=full-FT, surprise=96≈backbone, decision=similarity-gated)로 전환해야 삶. validator로 (1)embed-only≈full-FT? (2)s(τ)가 boundary 예측? 싸게 칠 것.

### 2026-06-22 KST 세션 14 (R14, b1/b2 non-efficiency, BL-19 PoC FAIL 직후 "가장 강한 1개") — 1 생성 (BL-19 inversion + self-diagnosis)
- [task-conditioning-locus](pending/r14_task-conditioning-locus.md) — frozen mt30에서 *학습 0*, per-module embedding **swap intervention**(not zero→OOD confound 제거). headline claim: **same-embodiment family 안에서**(physics 동일, reward만 변) dynamics에 다른 task emb 줘도 return 유지(R(dyn) 높음=label redundant)인데 reward에 같은 swap하면 붕괴(R(rew) 낮음=label 필수) → "frozen dynamics는 reward-variation은 transfer, physics-variation은 못 함". **BL-19 정확히 설명**(BL-19은 physics/질량 바꾸는 task에 frozen dyn 적응하려다 死=embodiment 넘음). decision(b2,후속/학습필요)=reward-variation onboarding은 dyn 안 풀어도 됨. bar4 PASS(단일 frozen model, 한 축만). risk=bar2(R(dyn)≈R(rew)면 死). NULL 사전등록+all-swapped premise+total-influence control(H=3 dyn-영향 함정). cheap PoC(premise+locus+probe) 학습 0 2-3h, decision arm은 별도. | status: pending | direction: b1/b2 | base: TD-MPC2 mt30-1M | venue: NeurIPS/ICLR/CoRL
  - 메타교훈: **advisor 2-pass 핵심.** (1) diagnostic("which module needs task?")은 INCREMENTAL 死→claim-led null-drop+swap-not-zero+decision-rule로 전환. (2) **"random other task" swap은 cross-embodiment confound("당연하지")라 surprise 희석 → same-embodiment family(physics 고정/reward 변)로 분리해야 confound-free surprise.** 추가 blocker 2개: task-ID 아니라 *embedding 벡터만* patch(action_mask가 ID indexing `world_model.py:159`), R(M)을 "task 무시"로 읽으려면 total-influence control 필수(H=3서 dyn 영향 작음/pi는 MPPI 보정 함정). efficiency 종결 후 b1/b2 survivor 후보 2호(R13 embed-only 死 → R14는 그 *정반대*+자기진단). PoC 학습 불필요라 BL-19/BL-14보다 싸고 빠름.

### 2026-06-22 KST 세션 12b (team-lead "GPU 벽 피하는 efficiency 1개", 3조건 명시) — 0, pending 미저장
- **결과 0 (bl19 파일 미생성, collision manufacturing 안 함).** team-lead가 벽 피할 3조건 제시(①큰 non-vectorized serial op 통째 skip ②batch/episode amortize ③train-step count 감소). base 코드로 각 door를 specific blocker까지 walk: 셋 다 닫힘. R12 efficiency-소진 결론을 **base-code 레벨에서 재확인.**
  - **door①+② = 코드검증 gradient-coupling kill (한 grep):** DreamerV3 `models.py:153` encoder가 gradient tape 안 → `:183` model_loss(decoder recon `:176`+KL) → `:191` `_model_opt(model_loss, self.parameters())`가 **매 step encoder 포함 전체 WM backprop**. encoder skip/cache = WM gradient sever, speedup이 끊는 양에 비례 = dual-rate 죽인 efficiency↔repr-quality coupling 동일. TD-MPC2도 `tdmpc2.py:24`(encoder가 같은 optimizer)+`:334 update`로 동일. + DreamerV3 큰 serial op는 encoder/decoder 둘뿐, decoder-skip=recon-free(R7선점). TD-MPC2(state-based)는 큰 serial op **자체 부재**(encoder=`layers.enc` 소형 MLP)→construction으로 닫힘.
  - **door③ = 선점+scope kill (다른 종류 blocker):** `common/buffer.py`=`SliceSampler`(uniform replay)라 error/disagreement-prioritized replay는 net-new→**MaPER/UPER 선점**(R4b) / UTD·replay-ratio=generic RL knob(no-port 규칙) / curriculum=quality drift(R12 rebrand 금지).
  - 메타교훈: **"GPU 벽 피하기"의 핵심 discriminator = 큰 serial op를 gradient-sever 없이 skip할 수 있는가** — 두 base 모두 큰 serial op(encoder/decoder)가 매 step gradient path에 있어 skip=gradient sever, 또는 애초에 부재(TD-MPC2). encoder-gradient-path **grep 한 번이 door①②를 동시에 닫음** — efficiency idea 생성 시 이 grep을 먼저 칠 것. R12 "framing 단위로 死"의 코드증거.

### 2026-06-22 KST 세션 15 (R14 FAIL(BL-20) 후 b1/b2/b3 efficiency 1개 요청) — 0 clean (literature closure of the one live spot)
- [round15-efficiency-sequential-depth-closure](pending/round15-efficiency-sequential-depth-closure.md) — b1/b2/b3 전부 efficiency framing. R12/R12b가 code-level로 닫은 efficiency lever의 **유일 survivor 타깃(planning sequential-depth, BL-15가 지명한 latency-bound 부분)을 R15 문헌검증으로 닫음.** discriminating check: wall-clock 떨어지는 통로 5종(call-skip/batch-reduction/small-op/adaptive-stop/gradient-skip) 전부 BL-10~18로 死, 유일 예외 sequential-depth는 3-attack 모두 occupied — adaptive-stop=AOP(1912.01188) / parallel-amortize=내 amortized-rollout-operator(closed-loop, INCREMENTAL) / **parallel-scan=PaMoRL(NeurIPS24, OpenReview R6N9AGyz13)** / **DEQ-iterate=Looped World Models(arXiv:2606.18208, R15 6일 전)**. PaMoRL이 closed-loop blocker("non-linear actor라 sequence-length 병렬화 불가")를 자백 = 정책-조건부 sequential rollout 병렬화 불가가 literature consensus, 우회로(policy 흡수)는 AOP가 선점+INCREMENTAL. | status: closed-lever-finding
  - 메타교훈: **efficiency는 메커니즘이 아니라 framing 단위로 죽는다(R12 재확인)** — 신규 메커니즘이 "wall-clock 떨어지는 5+1 통로" 밖으로 못 나감, 유일 통로(sequential-depth)도 R15에서 occupied 확정. **literature는 6일 전에도 움직인다**(LoopWM 2606.18208이 DEQ-iterate 갈래를 R15 6일 전 닫음 — pending-fresh≠literature-fresh, 생성 전 최신 arXiv 필수). R12=code-walk closure, R15=literature closure of the one survivor → lever 6개째 닫힘. forward: efficiency 종결(rebrand 금지 R10), 미테스트 lever=human-defined quality/sample-eff/capability problem-setting(R8 bet 공간), **validator wedge 해소가 새 생성보다 EV 높음**(5번째 escalation).

### 2026-06-22 KST 세션 15b (사용자가 direction i 선택 → quality/sample-eff/capability pivot) — 0 clean (un-absorbability gate, "none-passed-on-paper")
- [round15b-unabsorbability-gate-finding](pending/round15b-unabsorbability-gate-finding.md) — efficiency 종결 후 사용자가 direction (i)(quality/sample-eff/capability) 선택. **핵심 reframe: 이 space의 binding gate는 literature가 아니라 un-absorbability**(PoC-vs-resource-matched-baseline). 지난 4死(BL-14 critic-bootstrap / BL-19 full-FT / dual-rate size-matched / BL-20)가 *하나의 死 네 번* = "잘 튜닝된 WM+planner가 주입 구조를 같은 데이터로 흡수". un-absorbability test: matched baseline(같은 reward-free 데이터로 WM pretrain + return-driven planner)이 흡수 가능하면 死. **두 후보 死**: (1)controllability-whitening=WM-pretrain이 command→consequence 전체 흡수+WPT/XTRA 선점 / (2)low-level-internals-as-privileged=**Puppeteer 코드 반증**(`transfer.py:100` high_level_obs⊇low_level_obs, TD-MPC2 non-recurrent eval-deterministic → V_low=f(관측입력)=computable feature, privileged 아님, baseline이 aux-distill로 0-cost 흡수). | status: closed-gate-finding
  - 메타교훈: **quality/sample-eff의 binding gate = un-absorbability**(literature 아님, cell은 이미 mapped). lit check 후보당 1회. **"inject structure WM already has"는 fully-observed base에서 대부분 死**(reward-free에서 유도가능/나쁜방향 downweight/이미계산 target reweight = 자동 死). **"현재 안 wiring됨 ≠ un-absorbable"** — privileged 주장은 코드로 obs superset 먼저 확인(Puppeteer high_level_obs⊇low_level_obs). **dual-rate fallback 사라짐(parity, abandon)**+"dual-rate가 sample-eff로 이겼다"는 거짓(coordinator framing 오류, result_003.md=parity). 유일 open=(a)what-data-enters-buffer(crowded: Plan2Explore/curiosity, replay=MaPER/UPER). **validator wedge 해소가 EV 높음(5번째 escalation)**: pending 24+, conditional-go 18, survival=0.

### 2026-06-23 KST 세션 15c (coordinator가 b1 latent-compression + b2 planning-efficiency 재개) — 0 (없음, mechanism-triage closure)
- [round15c-b1-b2-mechanism-triage](pending/round15c-b1-b2-mechanism-triage.md) — efficiency 재개 요청. **핵심 논리: efficiency는 mechanism이 아니라 framing 레벨에서 죽었으므로(base=작은 vectorized op→launch-bound), named "untested" mechanism들은 base 구조를 안 바꾸는 한 닫힌 6-lever{call-skip/batch-reduction/small-op/adaptive-stop/gradient-skip/sequential-depth제거}로 환원.** gate-1(wall-clock)을 순서대로 먼저 적용→literature 도달 전 전부 닫힘. b1: dim-reduction=#3(BL-10) / factored-latent=BL-18(spectral, 정확히 이거) / quantized=launch-bound op엔 무이득 / info-bottleneck=quality objective miscategorize. b2: action-sampling=#2/#4(BL-15/16/AOP) / elite-selection=BL-15(정확히 cheap-rank) / algorithm변경(CEM)=dynamics 여전히 horizon-sequential(#6 PaMoRL/LoopWM/AOP) / **MuZero=launch-bound 아닌 (runnable-infra 부재[R10/R11 DreamerV4 동형]+already-probed subtree-reuse-muzero INCREMENTAL)로 死**. | status: closed-finding(없음)
  - 메타교훈: **mechanism-level "untested" 리스트는 framing-level kill을 탈출 못 함** — base 구조(vectorized→launch-bound) 안 바꾸면 모든 mechanism이 닫힌 6-lever 환원. **MuZero류는 launch-bound가 아니라 runnable-infra 부재+already-probed로 죽인다**(MCTS는 non-vectorized라 mislabel 주의, base 교체는 runnability 먼저). info-bottleneck처럼 quality objective를 efficiency로 miscategorize 주의. launch-bound vectorized base의 잔여 headroom=generic systems opt(kernel fusion/CUDA graph/precision)뿐=non-novel. R12 code-walk + R15 literature + R15c mechanism-triage = efficiency lever 7번째 closure, framing-dead 재확정.

### 2026-06-23 KST 세션 15d (coordinator가 what-data-enters-buffer 방향 시도) — 0 (없음, data-collection ceiling closure)
- [round15d-data-collection-ceiling-finding](pending/round15d-data-collection-ceiling-finding.md) — R15b가 지목한 유일 구조적-open 경로(what-data-enters-buffer). **결정 blade=occupancy 아니라 return ceiling**: 내 search가 직접 반환 — **Plan2Explore(Sekar 2020)가 task-reward access oracle(Dreamer)을 DMC zero-shot에서 거의 따라잡음/일부 능가**. 내 runnable base는 전부 dense-reward(walker_walk/cheetah_run/humanoid corridor target_vel=6.0/Stand·Walk·Run, sparse task 코드확인 부재). → matched baseline=WM+Plan2Explore collection. 새 collection은 un-absorbability 통과(다른 데이터)하나 **capture할 return headroom 없음**(dense에서 P2E-data≈oracle-data 천장) → un-absorbable AND novel AND 무이득. **efficiency와 동형 pincer**: dense base→ceiling死 / hard-exploration base(headroom 존재)→runnable-infra 부재→PoC불가→validator runnability gate死. 5각도 1-line: disagreement=P2E자체 / reward-aware=P2E가 이미 oracle매칭(dominated) / novelty=RND·count-based / pred-error=ICM / offline→online=offline-to-online MBRL+infra부재 / value-aware=VAML 2310.06253(R5 decision-fidelity 죽인 lit). | status: closed-finding(없음)
  - 메타교훈: **data-collection의 binding kill=return ceiling(dense-reward에서 P2E≈oracle), occupancy 아님.** un-absorbability는 necessary not sufficient(천장 있으면 死). closure 형태 결정 check=runnable infra에 sparse/hard-exploration task 있나→없음(30초 코드확인)→full pincer. **세 구조 카테고리 전부 종결: efficiency(framing-dead)+quality(un-absorbable)+data-collection(ceiling-capped).** forward(escalation 아님, 정확한 statement): generation lever가 현 runnable base 3종에서 소진. 그림 바꾸는 유일한 것=(1)headroom 있는 새 *runnable* base(sparse-reward, wall-clock 가능) (2)human-defined problem-setting(R8 bet, generator로 certify 불가).
